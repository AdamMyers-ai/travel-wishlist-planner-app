<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Travel Wishlist Planner</title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <%- include("partials/_navbar.ejs") %>

    <main class="container">
      <h1>Travel Wishlist Planner</h1>

      <p>
        Save destinations, set a priority & budget, and track trips from
        wishlist → planned → visited.
      </p>

      <% if(!user){ %>
      <div class="row">
        <a class="btn" href="/auth/sign-up">Get Started</a>
        <a class="btn" href="/auth/sign-in">Sign In</a>
      </div>
      <% } else { %>
      <a class="btn" href="/trips">Go to My Trips</a>
      <% } %>
    </main>
  </body>
</html>
