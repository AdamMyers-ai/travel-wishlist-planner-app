<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Travel Wishlist Planner</title>
  </head>
  <body>
    <%- include("partials/_navbar.ejs") %>

    <main>
      <h1>Travel Wishlist Planner</h1>

      <p>
        Save destinations, set a priority & budget, and track trips from
        wishlist → planned → visited.
      </p>

      <% if(!user){ %>
      <div class="row">
        <a href="/auth/sign-up" class="btn">Get Started</a>
        <a href="/auth/sign-in" class="btn-secondary">Sign In</a>
      </div>
      <% } else { %>
      <a href="/trips" class="btn">Go to My Trips</a>
      <% } %>
    </main>
  </body>
</html>
