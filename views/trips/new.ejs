<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>New Trip</title>
  </head>
  <body>
    <%- include("../partials/_navbar.ejs") %>

    <main class="container">
      <h1>Add a Trip</h1>

      <% if (message) { %>
      <p class="error"><%= message %></p>
      <% } %>

      <form action="/trips" method="post" class="card">
        <label for="destination">Destination *</label>
        <input type="text" name="destination" required />

        <label for="country">Country</label>
        <input type="text" name="country" />

        <label for="city">City</label>
        <input type="text" name="city" />

        <label for="status">Status</label>
        <select name="status">
          <option value="wishlist">Wishlist</option>
          <option value="planned">Planned</option>
          <option value="visited">Visited</option>
        </select>

        <label for="priority">Priority (1-5)</label>
        <input type="number" name="priority" min="1" max="5" value="3" />

        <label for="budget">Budget</label>
        <input type="number" name="budget" min="0" />

        <label for="travelMonth">Travel Month</label>
        <input type="text" name="travelMonth" placeholder="e.g. July 2026" />

        <label for="imageUrl">Image URL</label>
        <input name="imageUrl" placeholder="https://..." />

        <label for="notes">Notes</label>
        <textarea name="notes" rows="4"></textarea>

        <button class="btn" type="submit">Create Trip</button>
      </form>
    </main>
  </body>
</html>
